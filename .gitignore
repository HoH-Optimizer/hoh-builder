# ---------------------------------------------------------------------------
# Données personnelles — ne doivent JAMAIS être publiées.
# donnees.js contient l'inventaire complet d'un compte de jeu.
# ---------------------------------------------------------------------------
/donnees.js
/donnees.js.test
/data/
/heroes-of-history-export-*.json

# ---------------------------------------------------------------------------
# Fichiers de travail locaux
# ---------------------------------------------------------------------------
# Les captures d'écran servent à décoder le jeu, elles ne font pas partie du site.
# Attention : ne pas se contenter de /*.png, qui ne couvre que la racine.
*.png
!images/**
/Nouveau dossier/
/.claude/
node_modules/
.DS_Store
Thumbs.db
